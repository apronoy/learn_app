import 'package:flutter/material.dart';

class Step2Page extends StatelessWidget {
  const Step2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Step 2: Contact Info', style: TextStyle(fontSize: 20)),
    );
  }
}
