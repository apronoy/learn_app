import 'package:flutter/material.dart';

class Step4Page extends StatelessWidget {
  const Step4Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Step 4: Summary', style: TextStyle(fontSize: 20)),
    );
  }
}
