import 'package:flutter/material.dart';

class Step3Page extends StatelessWidget {
  const Step3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Step 3: Preferences', style: TextStyle(fontSize: 20)),
    );
  }
}
